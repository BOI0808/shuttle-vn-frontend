import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Xác nhận đặt sân' };

export default function BookingPage() {
  return <div>{/* BookingForm component sẽ được implement ở đây */}</div>;
}

import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Đặt sân không cần tài khoản' };

export default function GuestBookingPage() {
  return <div>{/* GuestBookingForm component sẽ được implement ở đây */}</div>;
}
